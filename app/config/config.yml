imports:
    - { resource: parameters.yml }
    - { resource: security.yml }
    - { resource: services.yml }

parameters:
    locale: en

framework:
    secret:          "%secret%"
    router:
        resource: "%kernel.root_dir%/config/routing.yml"
        strict_requirements: ~
    form:            ~
    csrf_protection: ~
    validation:      { enable_annotations: true }
    templating:
        engines: ['php']
    default_locale:  "%locale%"
    trusted_hosts:   ~
    trusted_proxies: ~
    session:
        handler_id:  ~
    fragments:       ~
    http_method_override: true

monolog:
    handlers: []

doctrine:
    dbal:
        driver:   pdo_mysql
        host:     "%database_host%"
        port:     "%database_port%"
        dbname:   "%database_name%"
        user:     "%database_user%"
        password: "%database_password%"
        charset:  UTF8

        types:
            evf_entity_id: Surfnet\Conext\OperationsSupportBundle\Doctrine\Type\EntityIdType
            evf_entity_type: Surfnet\Conext\OperationsSupportBundle\Doctrine\Type\EntityTypeType
            ops_jira_issue_status: Surfnet\Conext\OperationsSupportBundle\Doctrine\Type\JiraIssueStatusType
            ops_jira_issue_priority: Surfnet\Conext\OperationsSupportBundle\Doctrine\Type\JiraIssuePriorityType

    orm:
        auto_generate_proxy_classes: "%kernel.debug%"
        naming_strategy: doctrine.orm.naming_strategy.underscore
        auto_mapping: true

doctrine_migrations:
    dir_name: "%kernel.root_dir%/DoctrineMigrations"
    namespace: Surfnet\Conext\Migration
    table_name: migration_versions
    name: Conext Operations Support Migrations

swiftmailer:
    transport: "%mailer_transport%"
    host:      "%mailer_host%"
    username:  "%mailer_user%"
    password:  "%mailer_password%"
    spool:     { type: memory }

surfnet_jira_api_client:
    base_url: %jira_api_base_url%
    username: %jira_api_username%
    password: %jira_api_password%
    project_id: %jira_api_project_id%
    default_assignee: %jira_api_default_assignee%

surfnet_janus_api_client:
    base_url: %janus_api_base_url%
    username: %janus_api_username%
    password: %janus_api_password%

surfnet_conext_operations_support:
    suites:
        []
    jira:
        muted_status_id: %jira_muted_status_id%

doctrine_date_time_immutable_types:
    register: add
